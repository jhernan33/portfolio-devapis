# Dependencias solo para ejecutar los tests. NO entran en la imagen de
# producción: backend/Dockerfile instala únicamente requirements.txt.
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1

# Linter y formateador. Configuración en backend/ruff.toml.
ruff==0.16.5
